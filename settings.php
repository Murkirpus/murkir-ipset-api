<?php
/**
 * settings.php - Настройки для iptables.php v3.2
 *
 * Минимальный конфиг. Единственный обязательный параметр — API_BLOCK_KEY.
 * Остальные переопределяют дефолты из iptables.php (опционально).
 */

// =====================================================================
// ОБЯЗАТЕЛЬНО
// =====================================================================

// API-ключ для авторизации запросов. Используется для API и для веб-UI.
// Генерация сильного ключа: openssl rand -hex 24
define('API_BLOCK_KEY', '12345');

// =====================================================================
// ОПЦИОНАЛЬНО (раскомментируй если нужно переопределить)
// =====================================================================

// Таймаут бана по умолчанию в секундах.
// Через API можно передавать свой ?timeout=...
// define('BAN_TIMEOUT', 3600);

// Имена ipset-сетов. Менять только если есть конфликт с другими сетами.
// define('IPSET_V4', 'banlist4');
// define('IPSET_V6', 'banlist6');

// Максимум записей в сете (hash:ip).
// define('IPSET_MAXELEM', 1000000);
